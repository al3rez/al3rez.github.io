---
import { getEntry } from "astro:content";
import Page from "../components/page.astro";

const index = await getEntry("posts", "index");

if (!index) {
  throw new Error("Index post not found");
}
---

<Page post={index} index />
